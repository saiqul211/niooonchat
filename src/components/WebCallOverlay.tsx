import React from 'react';

/**
 * WebCallOverlay has been disabled per project requirements.
 * All voice and video calling interfaces are exclusively handled by
 * the native Material Android CallActivity with background foreground service support.
 */
export const WebCallOverlay: React.FC = () => {
  return null;
};
